<Window x:Class="PortariaApp.Views.SaidaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🚗 REGISTRO DE SAÍDA" Height="500" Width="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- TÍTULO -->
        <TextBlock Grid.Row="0" Text="👥 VISITANTES PENDENTES DE SAÍDA" 
                   FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"
                   Margin="0,0,0,20" Foreground="DarkBlue"/>

        <!-- DATAGRID -->
        <DataGrid x:Name="DataGridVisitantes" Grid.Row="1" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single" Margin="0,0,0,20">
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="👤 NOME" Binding="{Binding Nome}" Width="200"/>
                <DataGridTextColumn Header="🚗 PLACA" Binding="{Binding Placa}" Width="120"/>
                <DataGridTextColumn Header="⏰ ENTRADA" Binding="{Binding HoraEntrada, StringFormat=HH:mm:ss}" Width="100"/>
                <DataGridTextColumn Header="📱 TELEFONE" Binding="{Binding Telefone}" Width="130"/>
                <DataGridTextColumn Header="🏢 FILIAL" Binding="{Binding Filial}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- BOTÕES -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="MarcarSaidaBtn" 
                    Content="✅ MARCAR SAÍDA" 
                    Click="ButtonMarcarSaida_Click"
                    Width="140" Height="40" Margin="10"
                    Background="Orange" Foreground="White"
                    FontSize="14" FontWeight="Bold"/>
            
            <Button x:Name="FecharBtn" 
                    Content="❌ FECHAR" 
                    Click="FecharBtn_Click"
                    Width="100" Height="40" Margin="10"
                    Background="LightGray" Foreground="Black"
                    FontSize="14" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
